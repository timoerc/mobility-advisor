"""Deterministic compute layer: geocoding/route alternatives, fare calibration, trip
aggregation, the three-source trip-projection pipeline, per-trip subscription pricing,
portfolio simulation/ranking, the optimizer, and travel/annual-report statistics. Pure
compute — the store/ layer owns all fixture reads and writes."""
