# Environment & secrets
.env
.env.local
.env.*.local

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ADK / Google
.adk/

# Pipeline scratch files — regenerated on every analysis run by the trip-projection/
# simulation engine (tools.py); not fixture data, never meant to be committed.
mobility_advisor/data/_projected_trips_*.json
mobility_advisor/data/_optimization_results.json

# Timestamped data/ backups created by scenarios/activate_scenario.sh before it
# overwrites data/ with a scenario's fixtures.
mobility_advisor/data_backup_*/

# Logs
*.log

# Editor & OS
.DS_Store
Thumbs.db
ehthumbs.db
.idea/
.vscode/settings.json

# Claude Code (local session data)
.claude/
.adk/
CLAUDE.md
